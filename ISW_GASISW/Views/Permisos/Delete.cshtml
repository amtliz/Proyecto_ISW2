@model ISW_GASISW.Models.permisos

@{
    ViewBag.Title = "Eliminar - Permiso de Acceso";
}

<div class="panel panel-primary">
    <div class="panel-heading">
        <div class="panel-title">
            Eliminar - Permiso de Acceso
        </div>
    </div>
    <div class="panel-body">
        <h3>Esta seguro que desea eliminar este Permiso de Acceso?</h3>
        <fieldset>
            <div class="form-group">
                <div class="editor-label">
                    Nombre del Permiso de Acceso
                </div>
                <div class="display-field">
                    @Html.DisplayFor(model => model.nombre)
                </div>
            </div>
            <div class="form-group">
                <div class="editor-label">
                    Url del Permiso de Acceso
                </div>
            </div>
        </fieldset>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                <input type="submit" value="Eliminar" class="btn btn-primary" />
                <div class="btn btn-default">
                    @Html.ActionLink("Regresar", "Index")
                </div>
            </div>
        }
    </div>
</div>