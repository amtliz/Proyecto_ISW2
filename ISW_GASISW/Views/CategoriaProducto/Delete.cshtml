@model ISW_GASISW.Models.categoria_producto

@{
    ViewBag.Title = "Eliminar - Categoria de Producto";
}

<div class="panel panel-primary">
    <div class="panel-heading">
        <div class="panel-title">
            Eliminar - Categoria de Producto
        </div>
    </div>
    <div class="panel-body">
        <h3>Esta seguro que desea eliminar esta Categoria de Producto?</h3>
        <fieldset>
            <div class="form-group">
                <div class="editor-label">
                    Nombre de la Categoria de Producto
                </div>
                <div class="display-field">
                    @Html.DisplayFor(model => model.nombre)
                </div>
            </div>

        </fieldset>
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                <input type="submit" value="Eliminar" class="btn btn-primary" />
                <div class="btn btn-default">
                    @Html.ActionLink("Regresar", "Index")
                </div>
            </div>
        }
    </div>
</div>